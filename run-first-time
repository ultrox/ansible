# Done for manjaro
#!/bin/bash

# Ansible is python2 application you need 
# - python2
# - sshd running (thus openssh is installed)
# - deal with pacman asking for password when installing stuff

sudo pacman -S ansible sshpass openssh && \
sudo systemctl enable sshd && \
sudo systemctl start sshd
